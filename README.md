# library-management-system-
this a library management system that enable user to get the required book in terms title references by inputting there desired   book
design choices 
Encapulation private data+ public setters
single responsibility: Book handles its state, User handles own borrows,library orchestrates
in-memory storage- sufficient for most assignment
